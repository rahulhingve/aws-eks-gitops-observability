replicaCount: 1

image:
  repository: 5937930xxxx.dkr.ecr.ap-south-1.amazonaws.com/eks-demo-app
  pullPolicy: IfNotPresent
  tag: "521370c719ffdceffa54edecb7fb92a086ce5a23"

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

# ingress:
#   enabled: true
#   className: nginx
#   annotations:

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "200m"
    memory: "256Mi"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 70

livenessProbe:
  httpGet:
    path: /health
    port: 3000
  initialDelaySeconds: 10
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /health
    port: 3000
  periodSeconds: 10
  timeoutSeconds: 5
